

type MintCheckpoint @entity {
  id: ID!
  blockNumber: BigInt!
  hashrate: BigInt!

  difficulty: BigInt!
  epochCount: BigInt!
  challengeNumber: Bytes!
  maxSupplyForEra: BigInt!
  miningTarget: BigInt!
  tokensMinted: BigInt!
  minterAddress: Bytes!
}
